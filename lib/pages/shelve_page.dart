import 'package:flutter/material.dart';

class ShelvePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Center(
        child: new Text("书架"),
      ),
    );
  }
}
